{
  "name": "tiktok-shop-landing",
  "version": "1.0.0",
  "description": "TikTok-Shop style mobile-first sales landing page generator",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "serve": "node build.js && npx --yes serve dist"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
